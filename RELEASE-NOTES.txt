PRJ_VER=0.2.3
SUMMARY=Inserted git pull for each fedpkg targets;Fixed target: bodhi_new
BODHI_UPDATE_TYPE=bugfix
REDHAT_BUGZILLA=639816
[Changes]
- Inserted git pull for each fedpkg targets. Reduce the chance of conflict.
- Fixed target: bodhi_new. So it will actually run this command instead of
  just showing it.

